# RAGify — requirements.txt
# Python 3.13 compatible
# gradio is NOT listed here — HF Spaces installs it from sdk_version in README.md
# Adding it here would cause version conflicts.

pandas
openpyxl
xlrd
chardet
faiss-cpu
numpy
sentence-transformers
huggingface_hub>=0.24.0